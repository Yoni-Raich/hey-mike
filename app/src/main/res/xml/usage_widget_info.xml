<?xml version="1.0" encoding="utf-8"?>
<!-- Half-hourly is the platform floor. It is enough: the app redraws the widget
     on every new reading, and the timer only has to age the other accounts. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:description="@string/usage_widget_description"
    android:initialLayout="@layout/widget_usage"
    android:minWidth="250dp"
    android:minHeight="110dp"
    android:minResizeWidth="180dp"
    android:minResizeHeight="110dp"
    android:previewLayout="@layout/widget_usage"
    android:resizeMode="horizontal|vertical"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:updatePeriodMillis="1800000"
    android:widgetCategory="home_screen"
    tools:ignore="UnusedAttribute" />
