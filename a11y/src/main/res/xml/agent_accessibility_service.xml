<?xml version="1.0" encoding="utf-8"?>
<!--
  flagReportViewIds is mandatory: without it viewIdResourceName is null, the
  resourceId disappears from every node, and the primary addressing rule that
  every app card relies on stops working.

  The event mask is deliberately narrow. Only window and content changes are
  needed to tell when the screen has settled; typeAllMask would deliver every
  keystroke and text change in every app for no benefit.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/a11y_service_description"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeWindowsChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds|flagRetrieveInteractiveWindows|flagIncludeNotImportantViews"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:canTakeScreenshot="true"
    android:notificationTimeout="100" />
